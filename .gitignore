# Raw challenge data — distributed via the shared Google Drive per the
# Accenture x SUBAA brief; not versioned here (matches existing team convention).
/employees.csv
/attrition_log.csv
/engagement.csv
/performance.csv

# Cleaned data derived from the raw CSVs above — same rationale, regenerate
# with `python src/clean_data.py`. Large, reproducible, not versioned.
/data_clean/

# Run logs — transient, regenerated on every pipeline run.
/outputs/logs/

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python
__pycache__/
*.pyc

# Claude Code local tool config (not repo content)
.claude/
